/*
 * inventory_is_full()
 * returns a boolean which is true if the player's inventory is full
 * and false otherwise
 */
 
return player_controller_obj.player_inventory_size >= player_controller_obj.MAX_INVENTORY_SIZE;
