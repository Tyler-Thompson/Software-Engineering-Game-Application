/*
 * decrement_inventory_size()
 * this script should be called whenever an item is removed from 
 * the player's inventory.  It decrements the counter keeping track
 * of the inventory size
 */
 
player_controller_obj.player_inventory_size -= 1;

